/* PBJ by Tony https://repl.it/@tonymartins590/PBandJ-TonyMtxt */
/* Get Your Tools */
// Go to the counter. 
goTo(counter);
// Open the drawer.
open(drawer); // 
// Get a butter knife
get(butterKnife);
// Put the butter knife on the counter.
put(butterKnife,onCounter);
// Close the drawer.
close(drawer);
// Go to the cupBoard.
goTo(cupBoard);
// Open the cupBoard.
open(cupBoard);
// Get a plate.
get(plate);
// Close the cupBoard.
Close(cupBoard);
// Go to the counter.
goTo(counter);
// Put the plate onto the counter. 
put(plate,onCounter);
/* Get Your Ingredients */
// Open the bread bag.
open(breadbag);
// Get two bread slices.
get(breadSlice,2);
// Put the bread slices on the plate, 
// right and left.
put(breadSlice,2 onPlate);
// Close the bread bag.
Close(breadbag);
// Go to the pantry.
goTo(pantry);
// Open the pantry.
Open(pantry);
// Get the peanut butter jar.
Get(peanutButterjar);
// Hold the peanut butter jar. 
hold(peanutButterjar);
// Close the pantry.
close(pantry)
// Go to the counter.
goTo(counter);
// Put the jar of peanut butter on the counter.
put(peanutButterjar,onCounter);
// Go to the fridge.
goTo(fridge);
// Open the fridge.
Open(fridge);
// Get the jelly jar.
Get(Jellyjar);
// Hold the jelly jar. 
Hold(Jellyjar);
// Close the fridge. 
 Close(Cfridge);
// Go to the counter.
goTo(counter);
// Place the jelly jar on the counter.
Place(Jellyjar Counter);
/* Make the Sandwich */
// Get the peanut butter jar.
Get(peanutButterjar);
// Hold the peanut butter jar.
Hold(peanutButterjar)
// Open the peanut butter jar.
Open(peanutButterjar);
// Get the knife.
get(knife)
// Scoop peanut butter from the peanut butter jar.
Scoop(peanutbutter);
// Put the peanut butter jar on the counter. 
put(peanutButterjar, counter);
// Steady the left slice. 
steady(leftslice);
// Spread the peanut butter on the left slice.
Spread(peanutbutter,leftslice); // spread(peanutButter);
// Put the knife on the plate. 
put(knife,counter);
// Close the peanut butter jar.
close(peanutButterjar);
// Get the jelly jar.
get(Jellyjar)
// Hold the jelly jar.
Hold(Jellyjar)
// Open the jelly jar.
open(Jellyjar)
// Scoop jelly from the jelly jar.
scoop(jelly)
// Put the jelly jar down.
put(Jellyjar down)
// Steady the right slice.
steady(right breadSlice) 
// Spread the jelly on the right slice.
spread(jelly on right breadslice) 
// Get the left slice. 
get(leftslice)
// Turn the slice upside down.
Turn(slice upsidown) 
// Place the slice over the right slice. 
place(right slice over,rightslice)
// Eat the sandwich.
// /* Clean Up */
// Get the peanut butter jar. 
get(peanutButterjar)
// Hold the peanut butter jar. 
Hold(peanutButterjar)
// Go to the pantry.
goTo(pantry)
// Open the pantry.
open(pantry)
// Put the peanut butter jar in the pantry. 
put(peanutButterjar,pantry)
// Close the pantry.
close(pantry)
// Go to the counter. 
goTo(counter)
// Get the jelly jar. 
get(Jellyjar)
// Hold the jelly jar. 
Hold(Jellyjar)
// Go to the fridge.
goTo(fridge)
// Open the fridge.
open(fridge)
// Put the jelly jar in the fridge. 
put(Jellyjar in fridge)
// Close the fridge.
close(fridge)
// Go to the counter.
goTo(counter) 
// Get the knife. 
get(Knife)
// Hold the knife. 
hold(Knife)
// Go to the sink. 
goTo(Sink)
// Wash the knife. 
Wash(Knife)
// Go to the counter. 
goTo(counter)
// Open the drawer. 
Open(drawer)
// Put the knife in the drawer. 
Put(Knife,Drawer)